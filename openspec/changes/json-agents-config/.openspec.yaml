schema: spec-driven
created: 2026-08-07
goal: Move agent registry from hardcoded TypeScript array to a declarative
  JSON config file (agents-config.json), with env-var substitution and
  hot-reload. Inspired by kosmos-panel inventory.json pattern.
